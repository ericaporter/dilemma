= simple_form_for(:comment, url: comments_path(commentable_type: commentable.class.to_s.underscore, commentable_id: commentable.id)) do |f| 
  = f.label :content, label: "Comments" 
  = f.text_field :content
  = f.button :submit, "Post comment", class: 'expand'

